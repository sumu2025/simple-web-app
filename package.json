{
  "name": "simple-web-app",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 0",
    "build": "mkdir -p dist && echo Hello World > dist/index.html"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
